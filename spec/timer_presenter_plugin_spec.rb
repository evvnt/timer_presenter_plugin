RSpec.describe TimerPresenterPlugin do
  it "has a version number" do
    expect(TimerPresenterPlugin::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
